## Master Configuration
architecture: replication

auth:
  rootPassword: "replaced"
  replicationPassword: "replaced"
  database: "replaced"
  username: "replaced"
  password: "replaced"

primary:
  ## Master pod configuration
  nodeSelector:
    node: node-1
  
  persistence:
    enabled: true
    storageClass: "gp2"
    size: 1Gi
    accessModes:
      - ReadWriteOnce
  
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

secondary:
  ## Slave configuration
  replicaCount: 1
  
  nodeSelector:
    node: node-2
  
  persistence:
    enabled: true
    storageClass: "gp2"
    size: 1Gi
    accessModes:
      - ReadWriteOnce
  
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

## Metrics configuration (optional)
metrics:
  enabled: false

## Service configuration
service:
  type: ClusterIP
  port: 3306

## Network Policy
networkPolicy:
  enabled: false
